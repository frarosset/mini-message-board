<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mini Message Board</title>
  </head>
  <body>
    <%- include("header") _%>
    <main>
      <ul>
        <a href="/new">Post a new message</a>
        <% for (message of messages){ %>
        <li>
          <div>
            <span><%= message.user %></span>
            <span><%= dateToStr(message.added) %></span>
          </div>
          <p><%= message.text %></p>
        </li>
        <%}%>
      </ul>
      <div>
        <a href="/">RELOAD</a> Last updated: <%= (new Date().toLocaleString([],
        {})) %>
      </div>
    </main>
    <%- include("creditFooter") _%>
  </body>
</html>
